---
phase: 18-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/experts/loading.jsx
  - src/app/expert/[handle]/loading.jsx
  - src/app/answers/[id]/loading.jsx
  - src/app/trending/loading.jsx
  - src/app/questions/loading.jsx
  - src/app/topics/loading.jsx
  - src/app/topics/[slug]/loading.jsx
  - src/app/following/loading.jsx
autonomous: true
requirements:
  - PERF-01
  - PERF-03

must_haves:
  truths:
    - "Experts directory shows a content-shaped skeleton while data loads"
    - "Expert profile page shows a skeleton matching the profile layout while data loads"
    - "Answer detail page shows a skeleton while data loads"
    - "Trending, questions, topics, and following pages show skeletons while data loads"
    - "Users never see a blank white screen on any key page during initial load"
  artifacts:
    - path: "src/app/experts/loading.jsx"
      provides: "Skeleton for expert directory grid"
      exports: ["default"]
      min_lines: 15
    - path: "src/app/expert/[handle]/loading.jsx"
      provides: "Skeleton for expert profile page"
      exports: ["default"]
      min_lines: 15
    - path: "src/app/answers/[id]/loading.jsx"
      provides: "Skeleton for answer detail page"
      exports: ["default"]
      min_lines: 15
    - path: "src/app/trending/loading.jsx"
      provides: "Skeleton for trending page"
      exports: ["default"]
      min_lines: 10
  key_links:
    - from: "src/app/experts/loading.jsx"
      to: "Next.js Suspense boundary"
      via: "loading.jsx convention"
      pattern: "export default function"
---

<objective>
Add loading skeleton pages for all key public routes that currently show blank screens during data fetching.

Purpose: Eliminate perceived load time and CLS by showing content-shaped placeholders that match each page's final layout. This directly addresses PERF-03 (skeleton loaders) and contributes to PERF-01 (CLS < 0.1).

Output: 8 new loading.jsx files covering experts, expert profile, answer detail, trending, questions, topics, topic detail, and following pages.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing skeleton pattern (from src/app/loading.jsx): -->
```jsx
function Skeleton({ className }) {
  return <div className={`animate-pulse bg-warm-200 rounded ${className}`} />
}

export default function HomeLoading() {
  return (
    <div className="space-y-8">
      {/* Content-shaped skeletons matching final layout */}
    </div>
  )
}
```

<!-- Every loading.jsx defines its own local Skeleton helper. This is the established pattern. -->
<!-- Skeletons should match the actual page layout: same spacing, sizing, grid structure. -->

Existing loading.jsx files (follow these as pattern reference):
- src/app/loading.jsx (homepage: question card + answer cards + recent questions)
- src/app/q/[slug]/loading.jsx (question page: question + answer form + answer cards)
- src/app/search/loading.jsx (search: input + results list)

Pages needing loading.jsx (and their layout shapes):
- /experts — sort/filter bar + grid of expert cards (avatar + name + headline + stats)
- /expert/[handle] — profile header (large avatar + name + stats) + answer cards
- /answers/[id] — answer header (expert + question) + answer body + related answers
- /trending — page title + list of answer cards with engagement stats
- /questions — page title + list of question cards with dates and answer counts
- /topics — page title + grid of topic cards
- /topics/[slug] — topic header + list of question cards
- /following — two sections: followed experts grid + recent answers from followed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading skeletons for experts and expert profile pages</name>
  <files>src/app/experts/loading.jsx, src/app/expert/[handle]/loading.jsx, src/app/answers/[id]/loading.jsx</files>
  <action>
Create three loading.jsx files, each with a local Skeleton helper (matching the existing pattern — no shared component import).

1. `src/app/experts/loading.jsx` — Expert directory skeleton:
   - Sort/filter bar: row of 3-4 skeleton pills (h-8 w-20 each, rounded-full)
   - Grid of 6 expert cards: each card is a rounded-lg bordered container with:
     - Circular avatar skeleton (h-10 w-10 rounded-full)
     - Name line (h-4 w-32)
     - Headline line (h-3 w-48)
     - Stats row: 2 small blocks (h-3 w-16)
   - Use `grid grid-cols-1 sm:grid-cols-2 gap-4` to match actual layout

2. `src/app/expert/[handle]/loading.jsx` — Expert profile skeleton:
   - Profile header section:
     - Large circular avatar (h-16 w-16 rounded-full)
     - Name (h-7 w-48), headline (h-4 w-64), handle + stats row (h-3 w-40)
   - Bio section: 3 lines of text (h-4 w-full, h-4 w-full, h-4 w-2/3)
   - Stats cards: row of 3 small stat boxes (each h-16 w-full in 3-col grid)
   - Answer section header (h-5 w-32) + 2 answer card skeletons (matching homepage pattern)

3. `src/app/answers/[id]/loading.jsx` — Answer detail skeleton:
   - Question context: small category pill (h-3 w-20) + question text (h-7 w-full, h-7 w-3/4)
   - Expert header: avatar (h-10 w-10 rounded-full) + name (h-4 w-28) + date (h-3 w-20)
   - Answer body: 5 lines of text (h-4 w-full, varying widths on last line)
   - Action bar: row of 3 small icons (h-8 w-8 each)
   - Related answers section: header (h-5 w-40) + 2 compact answer skeletons
  </action>
  <verify>
    <automated>cd /Users/keithobrien/Desktop/Claude/Projects/ethos && test -f src/app/experts/loading.jsx && test -f "src/app/expert/[handle]/loading.jsx" && test -f "src/app/answers/[id]/loading.jsx" && echo "PASS"</automated>
  </verify>
  <done>Three loading.jsx files exist with content-shaped skeletons matching their respective page layouts.</done>
</task>

<task type="auto">
  <name>Task 2: Create loading skeletons for trending, questions, topics, and following pages</name>
  <files>src/app/trending/loading.jsx, src/app/questions/loading.jsx, src/app/topics/loading.jsx, src/app/topics/[slug]/loading.jsx, src/app/following/loading.jsx</files>
  <action>
Create five loading.jsx files, each with the local Skeleton helper pattern.

1. `src/app/trending/loading.jsx` — Trending page skeleton:
   - Page title placeholder (h-7 w-40)
   - Subtitle/description (h-4 w-64)
   - List of 4 answer cards: each with avatar (h-6 w-6 rounded-full) + expert name (h-3 w-24) + answer text (3 lines h-4) + engagement stats row (h-3 w-32)
   - Cards separated by dividers (border-b)

2. `src/app/questions/loading.jsx` — Questions list skeleton:
   - Page title (h-7 w-36)
   - List of 5 question cards: each bordered container with:
     - Date/category (h-3 w-24)
     - Question text (h-5 w-full, h-5 w-2/3)
     - Stats row: answer count + view count (h-3 w-40)

3. `src/app/topics/loading.jsx` — Topics listing skeleton:
   - Page title (h-7 w-32)
   - Grid of 6 topic cards (`grid grid-cols-2 sm:grid-cols-3 gap-4`):
     - Topic name (h-5 w-24)
     - Question count (h-3 w-16)
     - Follow button placeholder (h-8 w-20 rounded-full)

4. `src/app/topics/[slug]/loading.jsx` — Topic detail skeleton:
   - Topic header: name (h-8 w-48) + description (h-4 w-full, h-4 w-2/3) + follow button (h-9 w-24 rounded-full)
   - List of 4 question cards (same shape as questions/loading.jsx cards)

5. `src/app/following/loading.jsx` — Following page skeleton:
   - Section: "Experts you follow" header (h-6 w-40) + grid of 4 expert cards (avatar + name + headline, compact)
   - Section: "Recent from followed" header (h-6 w-48) + 3 answer card skeletons (avatar + expert name + answer preview)
  </action>
  <verify>
    <automated>cd /Users/keithobrien/Desktop/Claude/Projects/ethos && test -f src/app/trending/loading.jsx && test -f src/app/questions/loading.jsx && test -f src/app/topics/loading.jsx && test -f "src/app/topics/[slug]/loading.jsx" && test -f src/app/following/loading.jsx && echo "PASS"</automated>
  </verify>
  <done>Five loading.jsx files exist covering trending, questions, topics (listing + detail), and following pages.</done>
</task>

</tasks>

<verification>
1. `find src/app -name "loading.jsx" | wc -l` returns 11 (3 existing + 8 new)
2. Each loading.jsx exports a default function and uses the `animate-pulse bg-warm-200` skeleton pattern
3. `npm run build` passes (validates all loading.jsx files are valid React components)
4. No loading.jsx file imports from external packages (all self-contained with local Skeleton helper)
</verification>

<success_criteria>
- All 8 new loading.jsx files exist and follow the established skeleton pattern
- Every key public page (experts, expert profile, answer detail, trending, questions, topics, following) has a loading skeleton
- Skeletons match the content shape of their respective pages (not generic spinners)
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-performance/18-02-SUMMARY.md`
</output>
