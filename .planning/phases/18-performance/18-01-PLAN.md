---
phase: 18-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.mjs
  - src/components/Avatar.jsx
  - src/components/AnswerCard.jsx
  - src/components/CommentSection.jsx
  - src/app/page.jsx
  - src/app/expert/[handle]/page.jsx
  - src/app/experts/page.jsx
  - src/app/answers/[id]/page.jsx
  - src/app/trending/page.jsx
  - src/app/leaderboard/page.jsx
  - src/app/following/page.jsx
  - src/app/dashboard/page.jsx
  - src/app/admin/experts/page.jsx
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "All avatar images use next/image instead of raw <img> tags"
    - "External avatar domains (Google, LinkedIn) are configured in next.config.mjs remotePatterns"
    - "Avatar images have explicit width/height to prevent CLS"
    - "All avatar sizes use the Avatar component for consistency"
  artifacts:
    - path: "next.config.mjs"
      provides: "Remote image domain configuration for Google and LinkedIn avatars"
      contains: "remotePatterns"
    - path: "src/components/Avatar.jsx"
      provides: "Reusable Avatar component wrapping next/image with fallback initial"
      exports: ["default"]
      min_lines: 20
  key_links:
    - from: "src/components/Avatar.jsx"
      to: "next/image"
      via: "import Image from next/image"
      pattern: "import Image from.*next/image"
    - from: "src/components/AnswerCard.jsx"
      to: "src/components/Avatar.jsx"
      via: "import Avatar"
      pattern: "import Avatar"
---

<objective>
Replace all raw `<img>` avatar tags with `next/image` via a reusable Avatar component, and configure external image domains.

Purpose: Eliminate CLS from unsized images, enable automatic WebP/AVIF optimization for all avatar images, and reduce avatar payload via Next.js image optimization pipeline.

Output: Avatar.jsx component, next.config.mjs with remotePatterns, all 12+ files updated to use Avatar.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Avatar images come from OAuth providers (Google, LinkedIn). Rendered in 5 size variants: -->
<!-- 24px (w-6 h-6), 32px (w-8 h-8), 36px (w-9 h-9), 40px (w-10 h-10), 64px (w-16 h-16) -->
<!-- All use the same pattern: avatar_url ? <img> : <div with initial> -->
<!-- Sources: lh3.googleusercontent.com, media.licdn.com, *.linkedin.com -->

Current pattern (repeated 12+ times across codebase):
```jsx
{profile.avatar_url ? (
  <img src={profile.avatar_url} alt={profile.display_name} className="w-10 h-10 rounded-full object-cover" />
) : (
  <div className="w-10 h-10 rounded-full bg-warm-200 flex items-center justify-center text-warm-600 font-medium text-sm">
    {profile.display_name?.charAt(0)?.toUpperCase()}
  </div>
)}
```

Files with `<img>` avatar tags (all must be migrated):
- src/components/AnswerCard.jsx (line 71, 36px)
- src/components/CommentSection.jsx (line 12, 24px)
- src/app/page.jsx (lines 338, 390 — 32px and 48px)
- src/app/expert/[handle]/page.jsx (line 138, 64px)
- src/app/experts/page.jsx (line 244, 40px)
- src/app/answers/[id]/page.jsx (line 269, 32px)
- src/app/trending/page.jsx (line 64, 24px)
- src/app/leaderboard/page.jsx (line 85, 40px)
- src/app/following/page.jsx (line 113, 40px)
- src/app/dashboard/page.jsx (line 90, 64px)
- src/app/admin/experts/page.jsx (line 71, 32px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Avatar component and configure next/image domains</name>
  <files>next.config.mjs, src/components/Avatar.jsx</files>
  <action>
1. Update `next.config.mjs` to add `images.remotePatterns` for avatar provider domains:
   - `lh3.googleusercontent.com` (Google OAuth avatars)
   - `media.licdn.com` (LinkedIn avatars)
   - `*.licdn.com` (LinkedIn CDN variants)
   Keep it minimal — just add the images config to the existing empty config object.

2. Create `src/components/Avatar.jsx` — a reusable component that:
   - Accepts props: `src` (url or null), `alt` (string), `size` (number, default 40), `className` (optional extra classes)
   - When `src` is truthy: renders `<Image>` from `next/image` with `width={size}`, `height={size}`, `className="rounded-full object-cover"` plus any extra className, `alt` prop
   - When `src` is falsy: renders a `<div>` with same dimensions, rounded-full, warm-200 bg, centered initial letter from `alt` prop. Scale text size based on `size` (text-xs for <=24, text-sm for <=40, text-xl for >40).
   - The component should NOT be a client component (no 'use client' — Image works in server components). However, if consumed by a client component it will be fine.
   - Add `flex-shrink-0` to both variants to match existing styling.
  </action>
  <verify>
    <automated>cd /Users/keithobrien/Desktop/Claude/Projects/ethos && grep -q "remotePatterns" next.config.mjs && grep -q "next/image" src/components/Avatar.jsx && echo "PASS"</automated>
  </verify>
  <done>Avatar.jsx exports a component wrapping next/image with fallback. next.config.mjs allows Google and LinkedIn image domains.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all raw img avatar tags with Avatar component</name>
  <files>src/components/AnswerCard.jsx, src/components/CommentSection.jsx, src/app/page.jsx, src/app/expert/[handle]/page.jsx, src/app/experts/page.jsx, src/app/answers/[id]/page.jsx, src/app/trending/page.jsx, src/app/leaderboard/page.jsx, src/app/following/page.jsx, src/app/dashboard/page.jsx, src/app/admin/experts/page.jsx</files>
  <action>
In every file listed above, replace the `avatar_url ? <img> : <div>` pattern with the Avatar component.

Import pattern: `import Avatar from '@/components/Avatar'`

Replacement pattern (adapt per file):
```jsx
<Avatar src={profile.avatar_url} alt={profile.display_name} size={40} />
```

Size mapping (match existing Tailwind classes):
- `w-6 h-6` → `size={24}` (CommentSection, trending)
- `w-8 h-8` → `size={32}` (page.jsx answers, answers/[id], admin/experts)
- `w-9 h-9` → `size={36}` (AnswerCard)
- `w-10 h-10` → `size={40}` (experts, leaderboard, following)
- `w-12 h-12` → `size={48}` (page.jsx featured expert)
- `w-16 h-16` → `size={64}` (expert/[handle], dashboard)

For each file:
1. Add `import Avatar from '@/components/Avatar'` at the top (after existing imports)
2. Find each `{xxx.avatar_url ? (<img ...>) : (<div ...>)}` block
3. Replace entire ternary with single `<Avatar src={xxx.avatar_url} alt={xxx.display_name || 'Expert'} size={N} />`
4. Preserve any wrapper div/layout — only replace the img+fallback pattern

Special cases:
- `src/app/page.jsx` has TWO avatar patterns: one in recent answers (32px) and one in featured expert (48px). Replace both.
- `src/components/CommentSection.jsx` uses `comment.profiles?.avatar_url` and `comment.profiles?.display_name`.
- `src/app/expert/[handle]/page.jsx` already has `flex-shrink-0` on the img — Avatar includes this, so remove the extra class.
  </action>
  <verify>
    <automated>cd /Users/keithobrien/Desktop/Claude/Projects/ethos && ! grep -rn '<img.*avatar_url' src/ --include='*.jsx' | grep -v node_modules && echo "PASS: No raw img avatar tags remain"</automated>
  </verify>
  <done>Zero raw `<img>` tags for avatars remain in the codebase. All 12+ locations use the Avatar component with explicit sizes.</done>
</task>

</tasks>

<verification>
1. `grep -rn '<img' src/ --include='*.jsx'` returns zero results (no raw img tags anywhere)
2. `grep -rn 'next/image' src/components/Avatar.jsx` confirms Image import
3. `grep -c 'import Avatar' src/**/*.jsx` shows 11+ files importing Avatar
4. `npm run build` completes without errors (validates remotePatterns config and Image usage)
</verification>

<success_criteria>
- All avatar images use next/image via the Avatar component
- next.config.mjs has remotePatterns for Google and LinkedIn domains
- Every avatar has explicit width/height (via size prop) eliminating CLS
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-performance/18-01-SUMMARY.md`
</output>
