---
phase: 19-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/q/[slug]/error.jsx
  - src/app/answers/[id]/error.jsx
  - src/app/expert/[handle]/error.jsx
  - src/app/experts/error.jsx
  - src/app/topics/[slug]/error.jsx
  - src/app/dashboard/error.jsx
  - src/app/search/error.jsx
  - src/app/following/error.jsx
  - src/app/leaderboard/error.jsx
  - src/app/trending/error.jsx
  - src/app/questions/error.jsx
  - src/app/topics/error.jsx
autonomous: true
requirements:
  - UXP-01
  - UXP-02

must_haves:
  truths:
    - "When a page throws an error, users see a friendly error message with a retry button instead of a blank screen"
    - "Error boundaries exist for all key public route segments"
    - "Pages with no content show contextual guidance pointing users to a next action"
  artifacts:
    - path: "src/app/q/[slug]/error.jsx"
      provides: "Error boundary for question detail page"
    - path: "src/app/answers/[id]/error.jsx"
      provides: "Error boundary for answer page"
    - path: "src/app/expert/[handle]/error.jsx"
      provides: "Error boundary for expert profile page"
    - path: "src/app/dashboard/error.jsx"
      provides: "Error boundary for dashboard"
  key_links:
    - from: "src/app/*/error.jsx"
      to: "Next.js App Router error boundary system"
      via: "'use client' + error/reset props"
      pattern: "export default function.*Error.*\\{ error, reset \\}"
---

<objective>
Add route-level error boundaries for all key public and authenticated pages, and audit/enhance empty states to guide users toward their next action.

Purpose: Users should never see a blank screen or crash when a page fails to load. When pages have no content, users should get helpful guidance rather than bare "nothing here" text.

Output: error.jsx files for 12 route segments; improved empty states on pages that need better guidance.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing global error boundary pattern from src/app/error.jsx -->
'use client'

export default function GlobalError({ error, reset }) {
  return (
    <div className="flex flex-col items-center justify-center py-20 text-center">
      <h2 className="text-xl font-bold text-warm-900 mb-2">Something went wrong</h2>
      <p className="text-warm-500 text-sm mb-6 max-w-md">
        {error?.message || 'An unexpected error occurred.'}
      </p>
      <button
        onClick={reset}
        className="px-4 py-2 bg-accent-600 text-white rounded-md text-sm font-medium hover:bg-accent-700 transition-colors"
      >
        Try again
      </button>
    </div>
  )
}

<!-- Existing empty state pattern from src/app/following/page.jsx -->
<div className="text-center py-12">
  <p className="text-warm-600 mb-2">You're not following anyone yet.</p>
  <p className="text-warm-500 text-sm mb-4">
    Follow experts to see their answers here.
  </p>
  <Link href="/experts" className="inline-block px-4 py-2 bg-accent-600 text-white rounded-md text-sm font-medium hover:bg-accent-700 transition-colors">
    Discover experts
  </Link>
</div>

<!-- Pages with existing bare empty states that need enhancement (grep results) -->
<!-- src/app/leaderboard/page.jsx: "No answers yet. Check back soon." -->
<!-- src/app/questions/page.jsx: "No questions published yet." -->
<!-- src/app/topics/[slug]/page.jsx: "No questions tagged with this topic yet." -->
<!-- src/app/topics/page.jsx: "No topics yet." -->
<!-- src/app/experts/page.jsx: "No experts have answered yet." -->
<!-- src/app/trending/page.jsx: "No trending answers this week yet." -->
<!-- src/components/NotificationFeed.jsx: has decent guidance already -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create route-level error boundaries for all key pages</name>
  <files>
    src/app/q/[slug]/error.jsx
    src/app/answers/[id]/error.jsx
    src/app/expert/[handle]/error.jsx
    src/app/experts/error.jsx
    src/app/topics/[slug]/error.jsx
    src/app/topics/error.jsx
    src/app/dashboard/error.jsx
    src/app/search/error.jsx
    src/app/following/error.jsx
    src/app/leaderboard/error.jsx
    src/app/trending/error.jsx
    src/app/questions/error.jsx
  </files>
  <action>
    Create error.jsx files for each route segment listed. Follow the EXACT pattern from src/app/error.jsx (the global error boundary):

    - Must have `'use client'` directive at top
    - Must export default function accepting `{ error, reset }`
    - Must show friendly error message, error.message (or fallback), and a "Try again" button calling `reset()`
    - Use same Tailwind classes as the global error boundary for consistency
    - Add contextual messaging per page type:
      - Question/answer pages: "We couldn't load this content."
      - Expert profile: "We couldn't load this profile."
      - Directory pages (experts, topics, questions, leaderboard, trending): "We couldn't load this page."
      - Dashboard/following: "We couldn't load your data."
      - Search: "Search encountered an error."
    - Also include a "Go home" Link (import from next/link) as a secondary action below the Try again button, styled as text link: `text-sm text-accent-600 hover:text-accent-700`
  </action>
  <verify>
    <automated>find src/app -name "error.jsx" | wc -l</automated>
    Expected: 13 (12 new + 1 existing global)
  </verify>
  <done>All 12 route segments have error.jsx files with friendly error messages and retry buttons matching the global error boundary style</done>
</task>

<task type="auto">
  <name>Task 2: Enhance empty states with contextual guidance and CTAs</name>
  <files>
    src/app/leaderboard/page.jsx
    src/app/questions/page.jsx
    src/app/topics/page.jsx
    src/app/experts/page.jsx
    src/app/trending/page.jsx
  </files>
  <action>
    Audit and enhance the bare empty states on these pages. Follow the pattern from /following page (the gold standard empty state):

    1. **src/app/leaderboard/page.jsx** — Replace "No answers yet. Check back soon." with:
       - Primary text: "No answers on the leaderboard yet."
       - Secondary text: "Once experts start answering, you'll see the most active voices here."
       - CTA button linking to /experts: "Browse experts"

    2. **src/app/questions/page.jsx** — Replace "No questions published yet." with:
       - Primary text: "No questions published yet."
       - Secondary text: "New questions are published regularly. Check back soon or browse past questions."
       - CTA button linking to /: "Go to today's question"

    3. **src/app/topics/page.jsx** — Replace "No topics yet." with:
       - Primary text: "No topics yet."
       - Secondary text: "Topics will appear as questions are organized. Browse questions in the meantime."
       - CTA button linking to /questions: "Browse questions"

    4. **src/app/experts/page.jsx** — Replace "No experts have answered yet." with:
       - Primary text: "No experts have answered yet."
       - Secondary text: "When experts share their perspectives, they'll appear here."
       - CTA button linking to /: "See today's question"

    5. **src/app/trending/page.jsx** — Replace "No trending answers this week yet." with:
       - Primary text: "No trending answers this week."
       - Secondary text: "Trending answers are ranked by views and engagement. Check back as more people participate."
       - CTA button linking to /questions: "Browse all questions"

    Use the same structure for all:
    ```jsx
    <div className="text-center py-12">
      <p className="text-warm-600 mb-2">{primary}</p>
      <p className="text-warm-500 text-sm mb-4">{secondary}</p>
      <Link href={ctaHref} className="inline-block px-4 py-2 bg-accent-600 text-white rounded-md text-sm font-medium hover:bg-accent-700 transition-colors">
        {ctaLabel}
      </Link>
    </div>
    ```

    Make sure Link is imported from 'next/link' in each file (add import if not already present).
  </action>
  <verify>
    <automated>grep -r "text-center py-12" src/app/leaderboard/page.jsx src/app/questions/page.jsx src/app/topics/page.jsx src/app/experts/page.jsx src/app/trending/page.jsx | wc -l</automated>
    Expected: 5 (one per page)
  </verify>
  <done>All 5 pages show contextual guidance with descriptive text and a CTA button directing users to a logical next action, matching the established /following empty state pattern</done>
</task>

</tasks>

<verification>
1. `find src/app -name "error.jsx" | wc -l` returns 13
2. Each error.jsx has 'use client', error/reset props, Try again button, and Go home link
3. All 5 enhanced empty states have primary text, secondary guidance, and CTA button with Link
4. No broken imports — all files that use Link import it from 'next/link'
</verification>

<success_criteria>
- Every key public route segment has an error.jsx that catches rendering errors and shows a friendly retry UI
- Empty states on content-listing pages guide users toward a next action instead of just saying "nothing here"
- All error and empty state UI follows the established warm color palette and Tailwind patterns
</success_criteria>

<output>
After completion, create `.planning/phases/19-ux-polish/19-01-SUMMARY.md`
</output>
