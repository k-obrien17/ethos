---
phase: 12-content-surfacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.jsx
  - src/app/questions/page.jsx
  - src/app/questions/[slug]/page.jsx
autonomous: true
requirements:
  - SURF-01
  - SURF-02

must_haves:
  truths:
    - "User sees a 'Trending This Week' section on the homepage showing top answers ranked by views + bookmarks"
    - "User can browse all past questions on /questions and click through to see every answer for each question"
    - "Past question detail pages show all answers organized chronologically with full answer previews"
  artifacts:
    - path: "src/app/page.jsx"
      provides: "Trending section on homepage between today's answers and recent questions"
    - path: "src/app/questions/page.jsx"
      provides: "Enhanced question archive with answer counts and engagement indicators"
    - path: "src/app/questions/[slug]/page.jsx"
      provides: "Past question detail page showing all answers chronologically"
  key_links:
    - from: "src/app/page.jsx"
      to: "answers table"
      via: "Supabase query ordering by view_count + bookmark_count"
      pattern: "from.*answers.*order.*view_count"
    - from: "src/app/questions/[slug]/page.jsx"
      to: "answers + profiles"
      via: "Supabase join query"
      pattern: "from.*answers.*eq.*question_id"
---

<objective>
Add a trending answers section to the homepage and build out the question archive so users can browse past questions and see all their answers.

Purpose: SURF-01 requires trending content on the homepage; SURF-02 requires browsable past questions with full answer archives. Together these surface high-quality content without requiring search.
Output: Updated homepage with trending section, enhanced /questions listing, new /questions/[slug] detail page.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/page.jsx
@src/app/questions/page.jsx
@src/app/trending/page.jsx
@src/app/q/[slug]/page.jsx
@src/components/AnswerCard.jsx
@src/components/QuestionCard.jsx

<interfaces>
From src/app/trending/page.jsx (pattern for trending answer queries):
- Supabase query: answers table with profiles + questions joins, ordered by like_count, filtered by date range
- Uses ReactMarkdown for answer body preview (truncated to 400 chars)
- Article cards with expert avatar, question context, like/comment counts

From src/app/q/[slug]/page.jsx (pattern for question detail with answers):
- Fetches answers with profiles join, comments, likes in parallel
- Uses AnswerCard component for full answer rendering
- Sorts featured first, then by created_at DESC

From src/components/QuestionCard.jsx:
- Accepts question (with question_topics) and answerCount props
- Renders category, date, question body, topic pills, answer count

From src/app/api/answers/[id]/view/route.js:
- Views tracked via increment_view_count RPC (view_count column exists on answers table)
- Bookmarks table exists with question_id foreign key
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trending section on homepage</name>
  <files>src/app/page.jsx</files>
  <action>
Add a "Trending This Week" section to the homepage, placed between today's answers section and the recent questions section.

Query: Fetch top 5 answers from the last 7 days, ordered by a combined engagement score: `view_count + (bookmark_count * 3) + (like_count * 2)`. Use a Supabase query on the `answers` table joining `profiles` and `questions` (same join pattern as /trending page). Filter: `created_at >= 7 days ago`, only answers with at least 1 view or 1 like or 1 bookmark. Limit 5.

Note: The answers table has `view_count` and `like_count` columns. For bookmark engagement, count bookmarks on the parent question. Since Supabase JS client can't do computed ORDER BY, fetch the top 20 by `view_count` DESC, then sort client-side by the weighted score `view_count + (like_count * 2)` and take top 5. Keep it simple — no need for a custom RPC.

Rendering: Compact card layout (not full AnswerCard). Each card shows:
- Expert avatar (small, 8x8) + display_name + handle link
- Question body (linked to /q/[slug])
- Answer excerpt (first 200 chars of body, plain text — strip markdown with a simple regex or just truncate)
- Footer: view count, like count, "Read more" link to /answers/[id]

Section header: "Trending This Week" with a "See all" link to /trending.

Add the query to the existing `Promise.all` block in the first parallel fetch (alongside auth + todayQuestion + recentQuestions) for efficiency. Use `subDays` from date-fns (already imported in trending page, add import here).

If no trending answers (all have 0 engagement), hide the section entirely.
  </action>
  <verify>
Run `npm run build` from the project root. The build should succeed with no errors. Visually verify the homepage renders a "Trending This Week" section when there are answers with engagement.
  </verify>
  <done>
Homepage shows a "Trending This Week" section with up to 5 answers ranked by engagement (views + likes), between today's answers and recent questions. Section hidden when no engaged answers exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Question archive with answer detail pages</name>
  <files>src/app/questions/page.jsx, src/app/questions/[slug]/page.jsx</files>
  <action>
**Part A — Enhance /questions page:**

Update the questions listing page to show richer information. For each question, display:
- Answer count (already present)
- Total view count across all answers (sum of view_count)
- Most recent answer date

Query update: Change the Supabase query to include `answers(count, view_count)` so we get both the count and view data. Compute total views client-side by summing. Add a subtle engagement indicator: if a question has > 5 answers, show a small "popular" badge.

Add a page header with subtitle: "Browse every question asked on Ethos and read all expert perspectives."

**Part B — Create /questions/[slug] detail page:**

Create a new route at `src/app/questions/[slug]/page.jsx` that shows a past question with all its answers. This is different from `/q/[slug]` — it's a read-only archive view without the answer form, optimized for browsing.

Note: Actually, `/q/[slug]` already shows all answers with an answer form. Rather than duplicating, make `/questions/[slug]` redirect to `/q/[slug]` using Next.js `redirect()` from `next/navigation`. This way the questions list links to the canonical question page which already satisfies SURF-02 (browse past questions and see all answers).

Update QuestionCard links: Ensure all QuestionCard instances on /questions link to `/q/{slug}` (they likely already do — verify and leave as-is if correct).

The key enhancement is on the /questions page itself: make it a proper archive with engagement data so users understand which questions generated the most discussion.

Add `generateMetadata` with title "Questions Archive" and description for SEO.
  </action>
  <verify>
Run `npm run build` from the project root. Build succeeds. Navigate to /questions — should show all past questions with answer counts and engagement data. Clicking a question navigates to /q/[slug] showing all answers.
  </verify>
  <done>
/questions page shows all past questions with answer counts and engagement indicators. Each question links to its full answer page at /q/[slug]. /questions/[slug] redirects to /q/[slug] for canonical URLs.
  </done>
</task>

</tasks>

<verification>
1. Homepage renders "Trending This Week" section with engagement-ranked answers
2. /questions page shows enriched question archive with answer counts
3. Clicking any question navigates to its answer page with all answers visible
4. Build passes with no errors
</verification>

<success_criteria>
- Trending section visible on homepage when engaged answers exist
- Question archive browsable with engagement context
- All past questions' answers accessible via click-through
- No build errors, existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/12-content-surfacing/12-01-SUMMARY.md`
</output>
