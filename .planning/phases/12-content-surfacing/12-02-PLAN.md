---
phase: 12-content-surfacing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/answers/[id]/page.jsx
autonomous: true
requirements:
  - SURF-03

must_haves:
  truths:
    - "User sees 'More from this expert' section on an answer page linking to the expert's other answers"
    - "User sees 'Other perspectives on this question' section on an answer page showing peer answers"
    - "Related content links navigate to the correct answer and expert pages"
  artifacts:
    - path: "src/app/answers/[id]/page.jsx"
      provides: "Related content sections: more from expert + other answers to same question"
  key_links:
    - from: "src/app/answers/[id]/page.jsx"
      to: "answers table"
      via: "Supabase query filtering by expert_id != current answer"
      pattern: "from.*answers.*eq.*expert_id.*neq"
    - from: "src/app/answers/[id]/page.jsx"
      to: "answers table"
      via: "Supabase query filtering by question_id != current answer"
      pattern: "from.*answers.*eq.*question_id.*neq"
---

<objective>
Add "more from this expert" and "other answers to this question" related content sections to the answer detail page.

Purpose: SURF-03 requires related content links on answer pages so users can discover more content from the same expert and see peer perspectives on the same question. This creates natural content discovery paths.
Output: Enhanced answer page with two related content sections.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/answers/[id]/page.jsx
@src/app/expert/[handle]/page.jsx
@src/components/AnswerCard.jsx

<interfaces>
From src/app/answers/[id]/page.jsx (current structure):
- Fetches answer with profiles + questions joins
- Gets user auth, comments, like status
- Renders: back link, question context box, AnswerCard, "See all answers" link + ShareButton
- revalidate = 3600

From src/app/expert/[handle]/page.jsx (expert answer query pattern):
- Fetches all answers for an expert: supabase.from('answers').select('*, questions!inner(id, body, slug, category, publish_date)').eq('expert_id', profile.id)
- Shows answer list with question context, featured badge, ReactMarkdown body preview

Answer table columns available: id, body, word_count, like_count, comment_count, view_count, featured_at, created_at, expert_id, question_id
Profiles join: display_name, handle, avatar_url, headline
Questions join: id, body, slug, category, publish_date
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add related content sections to answer page</name>
  <files>src/app/answers/[id]/page.jsx</files>
  <action>
Add two related content sections to the answer detail page, placed after the existing "See all answers" link section and before the ViewTracker.

**Data fetching:**

Add two new parallel queries to the existing fetch logic (combine with the comments/likes fetch):

1. **More from this expert:** Fetch up to 3 other answers by the same expert (`expert_id = answer.profiles.id`), excluding the current answer (`id != answer.id`), ordered by `created_at DESC`. Include questions join for context: `questions!inner(body, slug, publish_date)`.

2. **Other perspectives on this question:** Fetch up to 4 other answers to the same question (`question_id = answer.questions.id`), excluding the current answer (`id != answer.id`), ordered by `like_count DESC` then `created_at DESC`. Include profiles join for expert info: `profiles!answers_expert_id_fkey(display_name, handle, avatar_url)`.

**Rendering — "More from this expert" section:**

Only render if there are other answers. Section header: "More from {expert.display_name}" with a "View all" link to `/expert/{handle}`.

Compact card list (not full AnswerCard — too heavy). Each card:
- Question body text (linked to `/q/{slug}`)
- Answer excerpt: first 150 chars of body, plain text
- Date + like count in footer
- Link to `/answers/{id}`

Use `divide-y divide-warm-100` for separation. Warm, minimal styling consistent with expert profile page's "All Answers" section.

**Rendering — "Other perspectives" section:**

Only render if there are other answers. Section header: "Other perspectives on this question" (no subtitle needed since the question context is already shown above).

Each card:
- Expert avatar (w-8 h-8) + display_name (linked to `/expert/{handle}`)
- Answer excerpt: first 200 chars of body, plain text
- Like count + "Read" link to `/answers/{id}`

Use `divide-y divide-warm-100` list layout.

**Replace the existing "See all answers" link section** (the div with flex items-center justify-center) with these two new sections. Move the "See all answers to this question" link into the "Other perspectives" section header as a "View all" link to `/q/{answer.questions.slug}`. Keep the ShareButton in a small utility bar between the AnswerCard and the related sections.

**Plain text excerpt helper:** Use a simple inline function to strip markdown: `const plainExcerpt = (text, len) => text.replace(/[#*_~`>\[\]()!|-]/g, '').slice(0, len).trim() + (text.length > len ? '...' : '')`. Define once at the top of the component function.
  </action>
  <verify>
Run `npm run build` from the project root. Build succeeds. Navigate to any answer page — should see "More from {expert}" and "Other perspectives" sections below the main answer.
  </verify>
  <done>
Answer page shows "More from this expert" section with up to 3 other answers by the same expert, and "Other perspectives" section with up to 4 other answers to the same question. Both sections link to full answer pages and expert profiles. Sections hidden when no related content exists.
  </done>
</task>

</tasks>

<verification>
1. Answer page shows "More from {expert name}" with links to other answers by the same expert
2. Answer page shows "Other perspectives on this question" with links to peer answers
3. Clicking related answer links navigates to correct /answers/[id] pages
4. Clicking expert name links navigates to correct /expert/[handle] pages
5. Sections gracefully hidden when no related content exists
6. Build passes with no errors
</verification>

<success_criteria>
- Both related content sections render on answer pages with real data
- Navigation between related answers creates a content discovery loop
- Empty states handled (sections hidden, not shown empty)
- No build errors, existing answer page functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/12-content-surfacing/12-02-SUMMARY.md`
</output>
