---
phase: 15-follow-experts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/experts/page.jsx
  - src/app/following/page.jsx
  - src/components/FollowButtonSmall.jsx
autonomous: true
requirements:
  - FLLW-01
  - FLLW-03

must_haves:
  truths:
    - "User can follow or unfollow an expert from the expert directory page, and the action persists across sessions"
    - "User can navigate to /following and see a list of all experts they follow with links to each expert's profile"
  artifacts:
    - path: "src/components/FollowButtonSmall.jsx"
      provides: "Compact follow/unfollow button for use inside directory cards without breaking Link wrapper"
    - path: "src/app/experts/page.jsx"
      provides: "Expert directory with inline follow buttons for authenticated users"
    - path: "src/app/following/page.jsx"
      provides: "Following page with expert list section above the recent answers feed"
  key_links:
    - from: "src/components/FollowButtonSmall.jsx"
      to: "src/app/actions/follows.js"
      via: "toggleFollow server action call"
      pattern: "toggleFollow"
    - from: "src/app/experts/page.jsx"
      to: "src/components/FollowButtonSmall.jsx"
      via: "FollowButtonSmall rendered inside each expert card"
      pattern: "FollowButtonSmall"
    - from: "src/app/following/page.jsx"
      to: "follows table"
      via: "Supabase query joining follows with profiles"
      pattern: "from.*follows"
---

<objective>
Add follow/unfollow capability to the expert directory and add an expert list section to the /following page.

The expert profile page already has a FollowButton. The expert directory (/experts) does not — each card is a Link element, so we need a small client-side follow button that stops propagation to avoid navigating when clicking follow. The /following page currently shows recent answers from followed experts but has no section listing the followed experts themselves with profile links.

Purpose: Complete FLLW-01 (follow from directory) and FLLW-03 (view list of followed experts).

Output: Expert directory cards include follow buttons; /following page has a "Your Experts" section above the answer feed.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/experts/page.jsx
@src/app/following/page.jsx
@src/components/FollowButton.jsx
@src/app/actions/follows.js

<interfaces>
Database tables (already exist):
  follows: follower_id UUID, following_id UUID, created_at TIMESTAMPTZ, PK(follower_id, following_id)
  profiles: id, handle, display_name, avatar_url, headline, organization, follower_count, following_count
  RLS: anyone SELECT, auth.uid() INSERT/DELETE on own rows

Server actions (src/app/actions/follows.js):
  toggleFollow(targetUserId) — validates auth, prevents self-follow, rate limits (30/15min), toggle insert/delete, updates counts, fires follow notification

Existing FollowButton (src/components/FollowButton.jsx):
  Props: targetUserId, isFollowing (initial boolean)
  Full-size button with optimistic toggle — used on expert profile page
  Cannot be used inside a Link without a stopPropagation wrapper

Expert directory (src/app/experts/page.jsx):
  Server component, each expert card is wrapped in <Link href={`/expert/${profile.handle}`}>
  Need a client component button inside the card that calls event.stopPropagation() and event.preventDefault() to prevent link navigation

Following page (src/app/following/page.jsx):
  Server component, queries follows table for following_ids then fetches recent answers
  Currently shows "X experts" count in header but no list of expert profiles
  Has a "Discover experts" link to /leaderboard — should change to /experts

Nav links to /following already exist in Header.jsx and MobileNav.jsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FollowButtonSmall component and add to expert directory</name>
  <files>src/components/FollowButtonSmall.jsx, src/app/experts/page.jsx</files>
  <action>
Create a compact follow button component that stops event propagation (for use inside Link wrappers).

1. Create `src/components/FollowButtonSmall.jsx`:
   - 'use client' component
   - Props: `targetUserId`, `isFollowing` (initial boolean)
   - Uses `useState` for optimistic toggle, `useTransition` for pending state
   - Calls `toggleFollow` from `@/app/actions/follows` on click
   - **Critical**: onClick handler must call `e.stopPropagation()` and `e.preventDefault()` before the toggle, so clicking the button doesn't navigate the parent Link
   - Style: small pill button, consistent with existing FollowButton styling but more compact
   - Show "Following" when following, "Follow" when not
   - Disabled during transition

2. Modify `src/app/experts/page.jsx`:
   - Add auth check: fetch current user at the top alongside existing parallel queries
   - If user is authenticated, fetch their follows list: `supabase.from('follows').select('following_id').eq('follower_id', user.id)`
   - Build a `Set` of followed expert IDs: `followedIds = new Set(follows.map(f => f.following_id))`
   - Convert each expert card from a pure `<Link>` to a `<Link>` that contains the expert info AND a `<FollowButtonSmall>` in the stats area (right side of the card)
   - Only render the FollowButtonSmall when user is authenticated and the expert is not the current user
   - Pass `isFollowing={followedIds.has(entry.id)}` and `targetUserId={entry.id}`
   - Import `FollowButtonSmall` at the top of the file
  </action>
  <verify>
    <automated>cd /Users/keithobrien/Desktop/Claude/Projects/ethos && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    FollowButtonSmall component created with stopPropagation for Link-safe usage.
    Expert directory shows follow/unfollow button on each expert card for authenticated users.
    Follow state persists via the existing follows table and toggleFollow server action.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add expert list section to /following page</name>
  <files>src/app/following/page.jsx</files>
  <action>
Add a "Your Experts" section to the /following page that shows all followed experts with links to their profiles.

In `src/app/following/page.jsx`:

1. After fetching `followingIds`, add a query to get profile details for all followed experts:
   ```
   const { data: followedProfiles } = await supabase
     .from('profiles')
     .select('id, handle, display_name, avatar_url, headline, organization, follower_count')
     .in('id', followingIds)
     .order('display_name', { ascending: true })
   ```

2. In the empty-state section (when followingIds.length === 0), change the "Discover experts" link from `/leaderboard` to `/experts`.

3. When there ARE followed experts, add a "Your Experts" section between the page header and the answer feed. Render it as a compact horizontal-scrollable or wrapped grid of expert cards:
   - Each card is a `<Link href={/expert/${profile.handle}}>` with avatar, display_name, headline, and follower_count
   - Style: compact cards in a `flex flex-wrap gap-3` container
   - Each card: `bg-white rounded-lg border border-warm-200 p-3 flex items-center gap-3 hover:border-warm-300 transition-colors`
   - Avatar (w-8 h-8), name (font-medium text-sm), subtitle (text-xs text-warm-500)

4. Below the expert list, keep the existing "Recent answers from followed experts" feed.

5. Add a visual separator (a simple label like "Recent from your experts") between the expert list and the answer feed.
  </action>
  <verify>
    <automated>cd /Users/keithobrien/Desktop/Claude/Projects/ethos && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    /following page shows "Your Experts" section with profile cards linking to expert profiles.
    Recent answers feed remains below the expert list.
    Empty state links to /experts for discovery.
  </done>
</task>

</tasks>

<verification>
- Build succeeds with no errors
- Expert directory page shows a follow/unfollow button on each expert card for authenticated users
- Clicking the follow button on a directory card does NOT navigate to the expert profile (stopPropagation works)
- Follow/unfollow persists across page reloads (backed by follows table)
- /following page shows a list of followed expert profiles with avatar, name, and link to /expert/[handle]
- /following page empty state links to /experts
- /following page still shows recent answers from followed experts below the expert list
- Self-follow button not shown (user's own card in directory has no button)
</verification>

<success_criteria>
1. User can follow or unfollow an expert from the expert directory, and the action persists across sessions
2. User can navigate to /following and see a list of all experts they follow, with links to each expert's profile
</success_criteria>

<output>
After completion, create `.planning/phases/15-follow-experts/15-01-SUMMARY.md`
</output>
